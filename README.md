# KiHiBooks : B2B2C 기반 웹소설 유통 및 통합 관리 플랫폼

## 프로젝트 개요
**KiHiBooks**는 여러 출판사가 입점하여 자사의 도서를 독립적으로 관리하고 일반 사용자가 이를 구매 및 열람 할 수 있는 B2B2C 통합 웹소설 플랫폼입니다. 
다수의 이해관계자(관리자, 출판사, 사용자)가 공존하는 환경에서 <strong>정교한 권한 체계(RBAC)</strong>를 구축하고, 입점사 간의 **데이터 격리** 및 **트랜잭션 안정성**을 확보하는 데 주력했습니다.
- **핵심 목표:** 입점사 간 데이터 보안이 보장되는 안전한 유통 환경 및 체계적인 도서 관리 시스템 구축
- **개발 인원:** 3인 (팀 프로젝트)
- **담당 역할:** RBAC 기반 권한 설계, 출판사별 데이터 격리 로직, 출판사 맞춤형 워크플로우 구현
---

## 사용 기술
* **Backend:** Java 17, Spring Boot, MyBatis, Spring Security
* **Database:** MySQL
* **Frontend:** Thymeleaf, JavaScript (ES6+), HTML5, CSS3

---

## 핵심 기능

* **RBAC 기반 다중 권한 관리**
    * **다층적 권한 설계:** 서비스 관리자, 출판사(대표/에디터), 일반 사용자로 역할을 세분화하여 기능 접근 권한 차등 부여.
    * **보안 계층화:** Spring Security와 securityFilterChain를 활용하여 URL 기반의 접근 제어를 설정하고, CustomUser 객체를 확장하여 세션 내에 출판사 코드(pu_code)와 식별값(pi_num)을 유지함으로써 모든 요청에서 즉각적인 권한 검증이 가능하도록 설계

* **역할별 기능 분담**
    * **관리자(ADMIN):** 서비스에 출판사 정식 등록 및 고유 출판사 코드(pu_code) 발급.
    * **출판사 대표(PUBLISHER_SUPER):** 사내 에디터 계정 생성 및 도서별 담당 에디터 지정으로 수정 권한 세분화.
    * **출판사 에디터(PUBLISHER_EDITOR):** 담당 도서의 에피소드 업로드, 가격 설정 및 작품 공지사항 관리.
    * **일반 사용자(USER):** 도서 검색, 구매 및 열람, 별점과 리뷰, 댓글 등 커뮤니티 활동.

* **출판사별 독립적 데이터 관리**
    * **소유권 검증 로직:** 세션의 출판사 코드와 리소스 소유권을 대조하는 검증 로직으로 타사 데이터 접근 원천 차단.
    * **에디터별 담당 도서 관리:** 도서별 담당 에디터 지정 기능을 통해 출판사 내부에서도 세밀한 수정 권한 통제.

 * **출판사 맞춤형 워크플로우 구현**
    * **콘텐츠 관리 프로세스:** 신규 도서 등록 → 에피소드 업로드 → 가격 설정 및 발행으로 이어지는 일련의 비즈니스 로직 구현.
    * **책임 관리 체계:** 도서별로 담당 에디터를 매핑하는 기능을 구현하여 출판사 내부에서도 담당자마다 본인이 맡은 작품만 관리할 수 있는 업무 환경 구성.
---

## 트러블슈팅

### 1. 다중 이해관계자 환경의 보안 취약점 개선
* **문제 상황**
    * Spring Security의 역할 기반 검증만으로는 타 출판사 에디터가 URL 파라미터 조작을 통해 타사 도서 데이터에 접근하는 행위를 막기 어려움.
    * 동일한 권한(에디터)을 가진 사용자 간에도 본인 소속 데이터만 다룰 수 있도록 하는 추가적인 소유권 검증 로직이 필요함.
* **기술적 해결**
    * **Spring Security 기반 인증 확장:** CustomUser 객체를 통해 세션 내에 출판사 코드(pu_code)를 상시 유지하여 요청마다 소유권을 즉시 대조할 수 있는 기반 마련.
    * **@PreAuthorize 통합 검증:** 컨트롤러 진입 전 세션의 출판사 코드와 요청된 리소스(도서/에피소드)의 소유주 코드가 일치하는지 확인하는 로직을 적용하여 코드 레벨의 보안 강화.
* **결과**
    * 실수나 악의적인 조작으로 타사 정보가 유출되는 사고 원천 차단.
    * B2B 플랫폼의 핵심인 입점사 간 데이터 격리와 보안 신뢰성 확보.

### 2. 복합 관리 로직의 데이터 정합성 유지
* **문제 상황**
    * 도서 등록 및 권한 위임 등 다중 테이블 수정 시, 예외 발생으로 일부 데이터만 반영되어 시스템 정합성이 깨질 위험 발생.
* **기술적 해결**
    * **트랜잭션 적용:** 서비스 레이어에 `@Transactional`을 적용하여 모든 과정이 완벽하게 성공할 때만 저장하고 하나라도 실패하면 롤백되도록 했습니다.
    * **선행 유효성 검사:** MyBatis 매핑 전 단계에서 파라미터 중복 체크 및 유효성 검사를 수행하여 비정상 데이터를 삽입 사전 차단했습니다.
* **결과**
    * 도서 회차를 등록하는 경우 썸네일과 도서 파일 중 데이터가 반만 저장되는 오류 없이 정상적인 유통과정 구축.

---
